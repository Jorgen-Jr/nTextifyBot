{
  "name": "ntextifybot",
  "version": "1.0.0",
  "description": "Bot for telegram at @ntextifybot that will modify texts.",
  "main": "server.js",
  "scripts": {
    "start": "node src/app.js",
    "dev": "nodemon src/app.js -ignore __tests__",
    "pretest": "NODE_ENV=test",
    "test": "NODE_ENV=test jest",
    "posttest": "NODE_ENV=test"
  },
  "author": "Jorge Antônio da Silva Nascimento Junior",
  "license": "ISC",
  "devDependencies": {
    "nodemon": "^1.19.4"
  },
  "dependencies": {
    "axios": "^0.21.1",
    "dotenv": "^8.2.0",
    "node-telegram-bot-api": "^0.50.0"
  }
}